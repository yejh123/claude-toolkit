# CODEBASE.md Template

Use this template to structure the output document. Every section is mandatory.
Adapt headings and depth to the project's complexity, but do not remove sections.

---

```markdown
# {Project Name} — Codebase Documentation

> Auto-generated by `understand-codebase` skill. Last updated: {date}.

## 1 Executive Summary

One paragraph: what the project does, who it's for, and why it exists.

## 2 Tech Stack

| Layer | Technology | Version | Purpose |
|-------|-----------|---------|---------|
| Language | e.g. Python | 3.12 | Core language |
| Framework | e.g. Flask | 3.x | Web framework |
| Database | e.g. PostgreSQL | 16 | Primary data store |
| Cache | e.g. Redis | 7.x | Session/cache store |
| ORM | e.g. SQLAlchemy | 2.x | Database abstraction |
| Task Queue | e.g. Celery | 5.x | Async task processing |
| Containerization | e.g. Docker | 24.x | Packaging & deployment |
| CI/CD | e.g. GitLab CI | — | Automated pipeline |
| Testing | e.g. pytest | 8.x | Test framework |
| Linter/Formatter | e.g. ruff | 0.8 | Code quality |

## 3 Project Structure

Annotated directory tree. Every directory gets a one-line explanation.

```
project-root/
├── app/                    # Application source code
│   ├── __init__.py         # Flask app factory, extension initialization
│   ├── models/             # SQLAlchemy models (one file per entity)
│   │   ├── user.py         # User model with auth fields
│   │   └── order.py        # Order model with payment status
│   ├── services/           # Business logic (no HTTP, no DB imports)
│   │   └── pricing.py      # Pricing calculation engine
│   ├── routes/             # Flask blueprints (HTTP layer only)
│   │   ├── auth.py         # Login, logout, register endpoints
│   │   └── api/            # REST API v1 endpoints
│   ├── tasks/              # Celery async tasks
│   └── utils/              # Pure utility functions (no side effects)
├── tests/                  # Mirrors app/ structure
│   ├── conftest.py         # Shared fixtures (db session, test client)
│   ├── test_models/
│   └── test_services/
├── migrations/             # Alembic database migrations
├── scripts/                # One-off scripts, data imports
├── config/                 # Environment-specific config files
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── .gitlab-ci.yml
└── CLAUDE.md
```

## 4 Architecture Overview

### 4.1 High-Level Architecture Diagram

```
┌─────────────┐     HTTPS      ┌──────────────┐     SQL      ┌────────────┐
│   Browser /  │───────────────▶│  Flask App   │────────────▶│ PostgreSQL │
│   Mobile App │◀───────────────│  (Gunicorn)  │◀────────────│            │
└─────────────┘    JSON/HTML    │              │              └────────────┘
                                │  ┌────────┐  │
                                │  │ Celery │  │──── Redis ──── Task Queue
                                │  │ Worker │  │
                                │  └────────┘  │
                                └──────────────┘
                                       │
                                       ▼
                                ┌──────────────┐
                                │   S3 / MinIO │  (File storage)
                                └──────────────┘
```

### 4.2 Design Decisions

Document the key architectural choices with WHY:

- **Why Flask over Django?** {explanation}
- **Why PostgreSQL over MySQL?** {explanation}
- **Why separate services/ from routes/?** {explanation}

## 5 Core Components

For each major module/package, document:

### 5.1 {Component Name}

**Location**: `app/services/pricing.py`

**Responsibility**: {One sentence — what this component does}

**Key Classes/Functions**:

| Name | Type | Purpose |
|------|------|---------|
| `PricingEngine` | Class | Calculates final price with discounts, tax, shipping |
| `apply_discount()` | Method | Applies coupon codes and tier-based discounts |
| `calculate_tax()` | Function | Determines tax based on jurisdiction |

**Dependencies**: Depends on `app.models.Order`, `app.utils.money`. No external API calls.

**Important Notes**:
```python
# 💡 DESIGN: Pricing is calculated at order creation AND at checkout.
#    This double-calculation guards against price changes between
#    cart addition and payment. The checkout price always wins.

# ⚠️ GOTCHA: Tax rates are cached in memory on startup.
#    If rates change, the service must be restarted.
#    TODO: Add a cache invalidation endpoint.
```

## 6 Data Model

### 6.1 Entity-Relationship Diagram

```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│     User     │       │    Order     │       │  OrderItem   │
├──────────────┤       ├──────────────┤       ├──────────────┤
│ id      (PK) │──┐    │ id      (PK) │──┐    │ id      (PK) │
│ email        │  │    │ user_id (FK) │  │    │ order_id(FK) │
│ name         │  └───▶│ status       │  └───▶│ product_id   │
│ role         │       │ total        │       │ quantity     │
│ created_at   │       │ created_at   │       │ unit_price   │
└──────────────┘       └──────────────┘       └──────────────┘
        │                                            │
        │              ┌──────────────┐              │
        │              │   Product    │              │
        │              ├──────────────┤              │
        └─────────────▶│ id      (PK) │◀─────────────┘
                       │ name         │
                       │ price        │
                       │ category_id  │
                       └──────────────┘
```

### 6.2 Model Details

For each model, list all fields with types, constraints, and purpose:

**User** (`app/models/user.py`):

| Field | Type | Constraints | Description |
|-------|------|------------|-------------|
| `id` | `Integer` | PK, auto-increment | Unique identifier |
| `email` | `String(255)` | Unique, not null, indexed | Login credential |
| `password_hash` | `String(255)` | Not null | bcrypt hash, never store plaintext |
| `role` | `Enum('user','admin')` | Default: 'user' | Authorization level |
| `created_at` | `DateTime` | Default: now(), not null | Account creation timestamp |

### 6.3 Database Migrations

Migration tool: {Alembic / Django / Prisma / ...}

```bash
# Generate a new migration after model changes
{exact command}

# Apply pending migrations
{exact command}

# Rollback last migration
{exact command}
```

## 7 API / Interface Reference

### 7.1 REST API Endpoints

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| `POST` | `/api/v1/auth/login` | No | Email + password login, returns JWT |
| `GET` | `/api/v1/users/me` | JWT | Current user profile |
| `GET` | `/api/v1/orders` | JWT | List user's orders (paginated) |
| `POST` | `/api/v1/orders` | JWT | Create a new order |

### 7.2 Request/Response Examples

```bash
# Login
curl -X POST http://localhost:5000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "user@example.com", "password": "secret"}'

# Response (200 OK):
{
  "access_token": "eyJhbG...",
  "token_type": "bearer",
  "expires_in": 3600
}
```

## 8 Request / Data Flow

Trace a complete operation from the user's action to the database and back.

### 8.1 Example: User creates an order

```
User clicks "Place Order"
       │
       ▼
[1] Browser sends POST /api/v1/orders
    Body: { items: [{product_id: 5, qty: 2}], coupon: "SAVE10" }
       │
       ▼
[2] Flask route: app/routes/api/orders.py :: create_order()
    - Validates JWT token (middleware)
    - Deserializes request body (Marshmallow schema)
    - Calls service layer
       │
       ▼
[3] Service: app/services/order_service.py :: create_order()
    - Fetches product prices from DB
    - Calls PricingEngine.calculate(items, coupon)
    - Creates Order + OrderItem records
    - Commits transaction
    - Enqueues Celery task: send_order_confirmation_email
       │
       ▼
[4] Database: INSERT INTO orders (...) + INSERT INTO order_items (...)
       │
       ▼
[5] Celery Worker picks up task (async)
    - Renders email template
    - Sends via SMTP
       │
       ▼
[6] Response returned to browser: 201 Created
    Body: { order_id: 42, status: "pending", total: 89.99 }
```

## 9 Configuration & Environment Variables

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| `DATABASE_URL` | Yes | — | PostgreSQL connection string |
| `SECRET_KEY` | Yes | — | Flask session signing key |
| `REDIS_URL` | No | `redis://localhost:6379/0` | Redis connection for cache/queue |
| `FLASK_ENV` | No | `production` | `development` enables debug mode |
| `SMTP_HOST` | No | — | Email server for notifications |
| `S3_BUCKET` | No | — | File upload storage bucket |
| `LOG_LEVEL` | No | `INFO` | Logging verbosity |

## 10 Getting Started

### 10.1 Prerequisites

List exact versions and installation commands:

```bash
# Required software
python >= 3.12
docker >= 24.0
docker-compose >= 2.20
```

### 10.2 Local Development Setup

Copy-paste runnable commands:

```bash
# Clone the repository
git clone git@gitlab.example.com:team/project.git
cd project

# Create virtual environment
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows

# Install dependencies
pip install -r requirements.txt
pip install -r requirements-dev.txt

# Set up environment variables
cp .env.example .env
# Edit .env with your local values

# Start infrastructure services
docker-compose up -d postgres redis

# Run database migrations
flask db upgrade

# Seed development data (if available)
python scripts/seed_dev_data.py

# Start the development server
flask run --reload --port 5000
# Server is now running at http://localhost:5000
```

### 10.3 Running Tests

```bash
# Run all tests
pytest

# Run with coverage
pytest --cov=app --cov-report=html

# Run a specific test file
pytest tests/test_services/test_pricing.py -v

# Run tests matching a pattern
pytest -k "test_discount" -v
```

### 10.4 Common Development Tasks

```bash
# Create a new database migration
flask db migrate -m "Add shipping_address to orders"

# Format code
ruff format app/ tests/

# Lint code
ruff check app/ tests/

# Type check
mypy app/
```

## 11 CI/CD Pipeline

Describe the pipeline stages, what each does, and how to trigger:

```
push to branch ──▶ lint ──▶ test ──▶ (MR only)
                                        │
merge to main ──▶ lint ──▶ test ──▶ build ──▶ deploy:staging ──▶ deploy:prod (manual)
```

Key files: `.gitlab-ci.yml` (or `.github/workflows/`)

## 12 Deployment

### 12.1 Production Architecture

```
┌─────────────┐     ┌──────────────┐     ┌────────────┐
│  Nginx /    │────▶│  Gunicorn    │────▶│ PostgreSQL │
│  Cloudflare │     │  (4 workers) │     │  (RDS)     │
└─────────────┘     └──────────────┘     └────────────┘
                           │
                    ┌──────┴──────┐
                    │ Celery      │──── Redis (ElastiCache)
                    │ (2 workers) │
                    └─────────────┘
```

### 12.2 Deployment Commands

```bash
# Build and push Docker image
docker build -t registry.example.com/project:latest .
docker push registry.example.com/project:latest

# Deploy to staging
{exact deploy command}

# Deploy to production
{exact deploy command}
```

## 13 Testing Strategy

| Test Type | Location | Runs When | What It Covers |
|-----------|----------|-----------|---------------|
| Unit | `tests/test_services/` | Every push | Business logic in isolation |
| Integration | `tests/test_routes/` | Every push | API endpoints + DB |
| E2E | `tests/e2e/` | Pre-deploy | Full user workflows |

## 14 Known Issues & Technical Debt

Be honest. List known problems:

- {Issue description, impact, and any workarounds}
- {Links to relevant issues/tickets if they exist}

## 15 Glossary

| Term | Definition |
|------|-----------|
| {Domain-specific term} | {Clear definition} |
```

---

Adapt the depth of each section to the project. A small CLI tool needs less than a full-stack web app. But never remove sections — if a section doesn't apply, write "Not applicable: {reason}".
